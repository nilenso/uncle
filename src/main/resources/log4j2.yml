Configuration:
  name: Default
  thresholdFilter:
    level: debug
  appenders:
    Console:
      name: STDOUT
      target: SYSTEM_OUT
      JsonTemplateLayout:
        eventTemplateUri: classpath:logLayout.json

  Loggers:
    Root:
      level: info
      AppenderRef:
        ref: STDOUT